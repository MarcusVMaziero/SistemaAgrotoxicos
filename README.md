# AplicacaoAgrotoxicos

```
git clone - para clonar o projeto
git add . - para adicionar tudo que foi alterado
git add nomeArquivo - para adicionar um arquivo especifico
git pull - para trazer tudo que foi alterado no projeto por outros desenvolvedores
git commit -m "Mensagem do seu commit"
git push origin master - para subir as suas alterações no repositório
```