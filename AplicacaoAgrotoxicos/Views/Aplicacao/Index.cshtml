@model IEnumerable<AplicacaoAgrotoxicos.Models.aplicacao>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.hecitares)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fk_produto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dataLimite)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.hecitares)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fk_produto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dataLimite)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
